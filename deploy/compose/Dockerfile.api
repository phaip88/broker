FROM golang:1.23-alpine AS build
WORKDIR /src
COPY go.mod ./
COPY cmd ./cmd
COPY internal ./internal
RUN CGO_ENABLED=0 go build -o /out/broker-api ./cmd/broker-api
FROM gcr.io/distroless/static-debian12
COPY --from=build /out/broker-api /broker-api
EXPOSE 8080
ENTRYPOINT ["/broker-api"]
